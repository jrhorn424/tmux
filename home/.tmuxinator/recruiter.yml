# ~/.tmuxinator/recruiter.yml

name: recruiter
root: ~/projects/recruiter

windows:
  - main: gs && git --no-pager log --oneline -5
  - git: git sh
  - server: bundle exec rails server
  - console: bundle exec rails console
  - jobs: bundle exec rake jobs:workoff
